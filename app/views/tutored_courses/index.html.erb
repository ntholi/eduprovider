<p id="notice"><%= notice %></p>

<h1>Tutored Courses</h1>

<table>
  <thead>
    <tr>
      <th>Course</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tutored_courses.each do |tutored_course| %>
    <tr>
      <td><%= tutored_course.course %></td>
      <td><%= link_to 'Show', tutored_course %></td>
      <td><%= link_to 'Edit', edit_tutored_course_path(tutored_course) %></td>
      <td><%= link_to 'Destroy', tutored_course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tutored Course', new_tutored_course_path %>
